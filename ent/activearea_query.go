// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"git.epam.com/epm-lstr/epm-lstr-lc/be/ent/activearea"
	"git.epam.com/epm-lstr/epm-lstr-lc/be/ent/predicate"
	"git.epam.com/epm-lstr/epm-lstr-lc/be/ent/user"
)

// ActiveAreaQuery is the builder for querying ActiveArea entities.
type ActiveAreaQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ActiveArea
	// eager-loading edges.
	withUsers *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ActiveAreaQuery builder.
func (aaq *ActiveAreaQuery) Where(ps ...predicate.ActiveArea) *ActiveAreaQuery {
	aaq.predicates = append(aaq.predicates, ps...)
	return aaq
}

// Limit adds a limit step to the query.
func (aaq *ActiveAreaQuery) Limit(limit int) *ActiveAreaQuery {
	aaq.limit = &limit
	return aaq
}

// Offset adds an offset step to the query.
func (aaq *ActiveAreaQuery) Offset(offset int) *ActiveAreaQuery {
	aaq.offset = &offset
	return aaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aaq *ActiveAreaQuery) Unique(unique bool) *ActiveAreaQuery {
	aaq.unique = &unique
	return aaq
}

// Order adds an order step to the query.
func (aaq *ActiveAreaQuery) Order(o ...OrderFunc) *ActiveAreaQuery {
	aaq.order = append(aaq.order, o...)
	return aaq
}

// QueryUsers chains the current query on the "users" edge.
func (aaq *ActiveAreaQuery) QueryUsers() *UserQuery {
	query := &UserQuery{config: aaq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(activearea.Table, activearea.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, activearea.UsersTable, activearea.UsersColumn),
		)
		fromU = sqlgraph.SetNeighbors(aaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ActiveArea entity from the query.
// Returns a *NotFoundError when no ActiveArea was found.
func (aaq *ActiveAreaQuery) First(ctx context.Context) (*ActiveArea, error) {
	nodes, err := aaq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{activearea.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aaq *ActiveAreaQuery) FirstX(ctx context.Context) *ActiveArea {
	node, err := aaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ActiveArea ID from the query.
// Returns a *NotFoundError when no ActiveArea ID was found.
func (aaq *ActiveAreaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aaq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{activearea.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aaq *ActiveAreaQuery) FirstIDX(ctx context.Context) int {
	id, err := aaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ActiveArea entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ActiveArea entity is found.
// Returns a *NotFoundError when no ActiveArea entities are found.
func (aaq *ActiveAreaQuery) Only(ctx context.Context) (*ActiveArea, error) {
	nodes, err := aaq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{activearea.Label}
	default:
		return nil, &NotSingularError{activearea.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aaq *ActiveAreaQuery) OnlyX(ctx context.Context) *ActiveArea {
	node, err := aaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ActiveArea ID in the query.
// Returns a *NotSingularError when more than one ActiveArea ID is found.
// Returns a *NotFoundError when no entities are found.
func (aaq *ActiveAreaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aaq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = &NotSingularError{activearea.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aaq *ActiveAreaQuery) OnlyIDX(ctx context.Context) int {
	id, err := aaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ActiveAreas.
func (aaq *ActiveAreaQuery) All(ctx context.Context) ([]*ActiveArea, error) {
	if err := aaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return aaq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (aaq *ActiveAreaQuery) AllX(ctx context.Context) []*ActiveArea {
	nodes, err := aaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ActiveArea IDs.
func (aaq *ActiveAreaQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := aaq.Select(activearea.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aaq *ActiveAreaQuery) IDsX(ctx context.Context) []int {
	ids, err := aaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aaq *ActiveAreaQuery) Count(ctx context.Context) (int, error) {
	if err := aaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return aaq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (aaq *ActiveAreaQuery) CountX(ctx context.Context) int {
	count, err := aaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aaq *ActiveAreaQuery) Exist(ctx context.Context) (bool, error) {
	if err := aaq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return aaq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (aaq *ActiveAreaQuery) ExistX(ctx context.Context) bool {
	exist, err := aaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ActiveAreaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aaq *ActiveAreaQuery) Clone() *ActiveAreaQuery {
	if aaq == nil {
		return nil
	}
	return &ActiveAreaQuery{
		config:     aaq.config,
		limit:      aaq.limit,
		offset:     aaq.offset,
		order:      append([]OrderFunc{}, aaq.order...),
		predicates: append([]predicate.ActiveArea{}, aaq.predicates...),
		withUsers:  aaq.withUsers.Clone(),
		// clone intermediate query.
		sql:    aaq.sql.Clone(),
		path:   aaq.path,
		unique: aaq.unique,
	}
}

// WithUsers tells the query-builder to eager-load the nodes that are connected to
// the "users" edge. The optional arguments are used to configure the query builder of the edge.
func (aaq *ActiveAreaQuery) WithUsers(opts ...func(*UserQuery)) *ActiveAreaQuery {
	query := &UserQuery{config: aaq.config}
	for _, opt := range opts {
		opt(query)
	}
	aaq.withUsers = query
	return aaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ActiveArea.Query().
//		GroupBy(activearea.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (aaq *ActiveAreaQuery) GroupBy(field string, fields ...string) *ActiveAreaGroupBy {
	group := &ActiveAreaGroupBy{config: aaq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := aaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return aaq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.ActiveArea.Query().
//		Select(activearea.FieldName).
//		Scan(ctx, &v)
//
func (aaq *ActiveAreaQuery) Select(fields ...string) *ActiveAreaSelect {
	aaq.fields = append(aaq.fields, fields...)
	return &ActiveAreaSelect{ActiveAreaQuery: aaq}
}

func (aaq *ActiveAreaQuery) prepareQuery(ctx context.Context) error {
	for _, f := range aaq.fields {
		if !activearea.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aaq.path != nil {
		prev, err := aaq.path(ctx)
		if err != nil {
			return err
		}
		aaq.sql = prev
	}
	return nil
}

func (aaq *ActiveAreaQuery) sqlAll(ctx context.Context) ([]*ActiveArea, error) {
	var (
		nodes       = []*ActiveArea{}
		_spec       = aaq.querySpec()
		loadedTypes = [1]bool{
			aaq.withUsers != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &ActiveArea{config: aaq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, aaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := aaq.withUsers; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*ActiveArea)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.Users = []*User{}
		}
		query.withFKs = true
		query.Where(predicate.User(func(s *sql.Selector) {
			s.Where(sql.InValues(activearea.UsersColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.active_area_users
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "active_area_users" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "active_area_users" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.Users = append(node.Edges.Users, n)
		}
	}

	return nodes, nil
}

func (aaq *ActiveAreaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aaq.querySpec()
	_spec.Node.Columns = aaq.fields
	if len(aaq.fields) > 0 {
		_spec.Unique = aaq.unique != nil && *aaq.unique
	}
	return sqlgraph.CountNodes(ctx, aaq.driver, _spec)
}

func (aaq *ActiveAreaQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := aaq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (aaq *ActiveAreaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   activearea.Table,
			Columns: activearea.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: activearea.FieldID,
			},
		},
		From:   aaq.sql,
		Unique: true,
	}
	if unique := aaq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := aaq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, activearea.FieldID)
		for i := range fields {
			if fields[i] != activearea.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aaq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aaq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aaq *ActiveAreaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aaq.driver.Dialect())
	t1 := builder.Table(activearea.Table)
	columns := aaq.fields
	if len(columns) == 0 {
		columns = activearea.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aaq.sql != nil {
		selector = aaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aaq.unique != nil && *aaq.unique {
		selector.Distinct()
	}
	for _, p := range aaq.predicates {
		p(selector)
	}
	for _, p := range aaq.order {
		p(selector)
	}
	if offset := aaq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aaq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ActiveAreaGroupBy is the group-by builder for ActiveArea entities.
type ActiveAreaGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aagb *ActiveAreaGroupBy) Aggregate(fns ...AggregateFunc) *ActiveAreaGroupBy {
	aagb.fns = append(aagb.fns, fns...)
	return aagb
}

// Scan applies the group-by query and scans the result into the given value.
func (aagb *ActiveAreaGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := aagb.path(ctx)
	if err != nil {
		return err
	}
	aagb.sql = query
	return aagb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := aagb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (aagb *ActiveAreaGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(aagb.fields) > 1 {
		return nil, errors.New("ent: ActiveAreaGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := aagb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) StringsX(ctx context.Context) []string {
	v, err := aagb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (aagb *ActiveAreaGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = aagb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = fmt.Errorf("ent: ActiveAreaGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) StringX(ctx context.Context) string {
	v, err := aagb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (aagb *ActiveAreaGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(aagb.fields) > 1 {
		return nil, errors.New("ent: ActiveAreaGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := aagb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) IntsX(ctx context.Context) []int {
	v, err := aagb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (aagb *ActiveAreaGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = aagb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = fmt.Errorf("ent: ActiveAreaGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) IntX(ctx context.Context) int {
	v, err := aagb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (aagb *ActiveAreaGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(aagb.fields) > 1 {
		return nil, errors.New("ent: ActiveAreaGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := aagb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := aagb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (aagb *ActiveAreaGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = aagb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = fmt.Errorf("ent: ActiveAreaGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) Float64X(ctx context.Context) float64 {
	v, err := aagb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (aagb *ActiveAreaGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(aagb.fields) > 1 {
		return nil, errors.New("ent: ActiveAreaGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := aagb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := aagb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (aagb *ActiveAreaGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = aagb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = fmt.Errorf("ent: ActiveAreaGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (aagb *ActiveAreaGroupBy) BoolX(ctx context.Context) bool {
	v, err := aagb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (aagb *ActiveAreaGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range aagb.fields {
		if !activearea.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := aagb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aagb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (aagb *ActiveAreaGroupBy) sqlQuery() *sql.Selector {
	selector := aagb.sql.Select()
	aggregation := make([]string, 0, len(aagb.fns))
	for _, fn := range aagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(aagb.fields)+len(aagb.fns))
		for _, f := range aagb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(aagb.fields...)...)
}

// ActiveAreaSelect is the builder for selecting fields of ActiveArea entities.
type ActiveAreaSelect struct {
	*ActiveAreaQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (aas *ActiveAreaSelect) Scan(ctx context.Context, v interface{}) error {
	if err := aas.prepareQuery(ctx); err != nil {
		return err
	}
	aas.sql = aas.ActiveAreaQuery.sqlQuery(ctx)
	return aas.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (aas *ActiveAreaSelect) ScanX(ctx context.Context, v interface{}) {
	if err := aas.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (aas *ActiveAreaSelect) Strings(ctx context.Context) ([]string, error) {
	if len(aas.fields) > 1 {
		return nil, errors.New("ent: ActiveAreaSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := aas.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (aas *ActiveAreaSelect) StringsX(ctx context.Context) []string {
	v, err := aas.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (aas *ActiveAreaSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = aas.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = fmt.Errorf("ent: ActiveAreaSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (aas *ActiveAreaSelect) StringX(ctx context.Context) string {
	v, err := aas.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (aas *ActiveAreaSelect) Ints(ctx context.Context) ([]int, error) {
	if len(aas.fields) > 1 {
		return nil, errors.New("ent: ActiveAreaSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := aas.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (aas *ActiveAreaSelect) IntsX(ctx context.Context) []int {
	v, err := aas.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (aas *ActiveAreaSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = aas.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = fmt.Errorf("ent: ActiveAreaSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (aas *ActiveAreaSelect) IntX(ctx context.Context) int {
	v, err := aas.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (aas *ActiveAreaSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(aas.fields) > 1 {
		return nil, errors.New("ent: ActiveAreaSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := aas.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (aas *ActiveAreaSelect) Float64sX(ctx context.Context) []float64 {
	v, err := aas.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (aas *ActiveAreaSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = aas.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = fmt.Errorf("ent: ActiveAreaSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (aas *ActiveAreaSelect) Float64X(ctx context.Context) float64 {
	v, err := aas.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (aas *ActiveAreaSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(aas.fields) > 1 {
		return nil, errors.New("ent: ActiveAreaSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := aas.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (aas *ActiveAreaSelect) BoolsX(ctx context.Context) []bool {
	v, err := aas.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (aas *ActiveAreaSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = aas.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{activearea.Label}
	default:
		err = fmt.Errorf("ent: ActiveAreaSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (aas *ActiveAreaSelect) BoolX(ctx context.Context) bool {
	v, err := aas.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (aas *ActiveAreaSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := aas.sql.Query()
	if err := aas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
